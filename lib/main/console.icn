package io

class Console()
   # Return a character from the console.  If flag {echo} is set, then output is echoed.
   public static defer get_char(echo)

   # Check to see if there is a keyboard character waiting to be read.
   public static defer wait_char()

   # Get the console size as a list, [rows, columns]
   public static defer get_size()
end
