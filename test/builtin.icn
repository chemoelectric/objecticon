procedure main()
   local l,f,s
   l := [
         abs,
         acos,
         any,
         args,
         asin,
         atan,
         bal,
         callout,
         cast,
         center,
         char,
         chdir,
         classof,
         cofail,
         collect,
         constructor,
         constructorof,
         copy,
         cos,
         cset,
         delay,
         delete,
         detab,
         display,
         dtor,
         entab,
         errorclear,
         event,
         eventmask,
         exec,
         exit,
         exp,
         fieldnames,
         find,
         fork,
         function,
         get,
         getch,
         getche,
         getenv,
         gettimeofday,
         glob,
         globalnames,
         iand,
         icom,
         image,
         insert,
         integer,
         ior,
         is,
         ishift,
         ixor,
         kbhit,
         key,
         keyof,
         keyword,
         kill,
         left,
         list,
         load,
         loadfunc,
         localnames,
         log,
         many,
         map,
         match,
         max,
         member,
         min,
         move,
         name,
         numeric,
         opmask,
         ord,
         paramnames,
         parent,
         pop,
         pos,
         proc,
         progof,
         pull,
         push,
         put,
         real,
         repl,
         reverse,
         right,
         rtod,
         runerr,
         seq,
         serial,
         set,
         setenv,
         sin,
         sort,
         sortf,
         sqrt,
         staticnames,
         string,
         structure,
         subtype,
         syserr,
         system,
         tab,
         table,
         tan,
         trap,
         trim,
         type,
         upto,
         variable,
         wait

         ]

   every f := !l do {
      yes_no{image(f), f === proc(subtype(f),0)}
   }

   every s := function() do
      yes_no{s, s === subtype(proc(s,0))}

end
