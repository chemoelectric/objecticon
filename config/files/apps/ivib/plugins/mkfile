< ../../../mkdefs

PROGSRC=`{ls *.icn}
PROGU=${PROGSRC:%.icn=%.u}
PROGS=${PROGSRC:%.icn=%}

all:V: $PROGS

clean:
	rm -f $PROGS *.u packages.txt

%: %.icn
        OI_PATH=($OI_PATH $OI_HOME/apps/ivib)
	oit -so $stem $stem.icn
