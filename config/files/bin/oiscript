#!/bin/sh
if [ -z "$OIXCACHE" ] ; then
    OIXCACHE=/tmp/oixcache
fi
mkdir -p $OIXCACHE
HASH=$OIXCACHE/`md5sum "$1" | cut -c1-32`
if [ ! -x $HASH ] ; then
    if ! oit -s -o $HASH - <"$1" ; then
        exit 1
    fi
fi
shift
$HASH "$@"
