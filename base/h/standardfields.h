/*
 * Standard field lookup fields.  Each is an instance into progstate's
 * standard_fields array, and the entry there gives an index into the
 * progstate's field table which corresponds to the particular field,
 * or -1 if absent.
 */
#define INIT_FIELD          0
#define NEW_FIELD           1
