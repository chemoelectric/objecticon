< ../mkdefs

PROGS=  ieval fiximports
PROGSRC=${PROGS:%=%.icn}

default:V:	all

all :   $PROGS
	@{ cd ivib ; mk all }
	@{ cd oidoc ; mk all }

clean :
	rm -f $PROGS *.u packages.txt
	@{ cd ivib ; mk clean }
	@{ cd oidoc ; mk clean }

%: %.icn
	oit -so $stem $stem.icn
	cp $stem $OI_BIN
